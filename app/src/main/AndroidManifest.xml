<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.carrey.animation" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AnimationList"/>
        <activity android:name=".AnimationDetal"/>
        <activity android:name=".PropertyAnimationAct"/>
        <activity android:name=".PropertyDetalAct"/>
        <activity android:name=".PropertyxnlAnimationAct"/>
        <activity android:name=".PropertyxmlDetalAct"/>
        <activity android:name=".TransitionActivity"/>
    </application>

</manifest>
